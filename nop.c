#include "monty.h"

/**
  * nop - does nothing
  * @stack: head of the stack
  * @num_line: line on which the error occurred
  * Return: Nothing
  */
void nop(stack_t **stack, unsigned int num_line)
{
	(void) num_line;
	(void) stack;
}
